apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fraud-worker.fullname" . }}-test-connection"
  labels:
    {{- include "fraud-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fraud-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
