apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-worker.fullname" . }}
  labels:
  {{- include "fraud-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
  {{- include "fraud-worker.selectorLabels" . | nindent 4 }}
